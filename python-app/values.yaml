image:
  repository: thedevopsdude/python-app
  tag: 997fc7e
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

serviceAccount:
  create: false

autoscaling:
  enabled: false

ingress:
  enabled: false
