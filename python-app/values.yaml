image:
  repository: thedevopsdude/python-app
  tag: 6d3c721
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

serviceAccount:
  create: false

autoscaling:
  enabled: false

ingress:
  enabled: false
