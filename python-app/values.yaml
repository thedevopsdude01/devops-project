image:
  repository: thedevopsdude/python-app
  tag: d8db413
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

serviceAccount:
  create: false

autoscaling:
  enabled: false

ingress:
  enabled: false
